#include <stdio.h>

int main(int argc, char ** argv)
{
  int ii;
  fprintf(stdout, "Hello, my name is %s and I have %d arguments:\n",
	  argv[0], argc);
  for(ii = 0; ii < argc; ++ii)
    fprintf(stdout, "  %d is %s\n", ii, argv[ii]);
  return 0;
}
